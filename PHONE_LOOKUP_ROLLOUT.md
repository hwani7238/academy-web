# 뒷번호 검색 적용

학생의 `phoneLast4` 필드에 숫자 문자열 네 자리를 저장하고 `/api/world/login`에서 동등 조건으로 조회한다. 같은 뒷번호의 학생은 모두 반환한다. 앞자리 0을 유지하며 잘못된 요청은 DB 조회 전에 거부한다. 조회 결과의 원래 전화번호도 다시 비교하여 오래된 검색 필드가 다른 학생의 자료를 반환하지 않게 한다.

이번 변경은 조회 효율 개선이다. 기존 피드백 응답 형식은 유지하며 4자리 인증, 공개 로그 규칙 등 앞서 지적한 보안 문제를 해결한 것은 아니다. 학생 본인/보호자 번호를 여러 개 등록하는 기능 및 출석·차감 기능은 포함하지 않는다.

## 운영 적용 순서

운영 자격증명은 저장소나 채팅에 넣지 않는다. 현재 로컬에는 운영 환경변수가 없어 DB 작업 및 배포는 실행하지 않았다.

1. 학생 자료 백업을 확인한다.
2. **등록/수정 경로부터 배포한다.** StudentManager, phone.mjs 및 일괄 등록 스크립트를 먼저 적용하고, 이 단계에는 world/login 조회 변경을 아직 배포하지 않는다. 열려 있는 기존 관리 화면은 새로고침한다.
3. 환경변수가 설정된 서버에서 `node scripts/backfill-phone-last4.mjs`를 실행한다. 기본은 dry-run이며 건수만 출력한다. 잘못된 번호 건수(`invalid`)가 있으면 기존 학생 정보를 확인한다.
4. `node scripts/backfill-phone-last4.mjs --apply`로 기존 자료의 검색 필드를 채운다. 각 문서를 트랜잭션으로 다시 읽어 동시 수정된 번호를 반영한다. 문서가 삭제되었으면 건너뛴다. 개인정보 값은 출력하지 않는다.
5. dry-run을 다시 실행하여 `changed: 0`을 확인한다. 이 필드는 Firestore의 기본 단일 필드 인덱스를 사용한다. 운영에서 students.phoneLast4의 인덱싱을 제외한 설정이 없는지 확인한다.
6. world/login 변경을 배포하고 지정된 테스트 학생으로 확인한다. 같은 뒷번호의 복수 학생, 앞자리 0, 없는 번호, 번호 변경을 확인한다. 로그 조회는 유지되므로 비용 절감 범위는 학생 검색 단계다.

현재 등록 화면 이외에 Firebase 콘솔이나 다른 도구에서 phone을 바꾸는 작업이 있으면 phoneLast4도 함께 갱신해야 한다. 추후 서버에서 학생 쓰기를 통합하면 이 규칙을 한곳에서 강제할 수 있다. 구버전 클라이언트가 남아 있는 경우 backfill을 다시 실행한 뒤 점검한다.

문제가 있으면 조회 핸들러만 이전 버전으로 되돌릴 수 있다. 추가된 phoneLast4 필드는 이전 버전과 호환되므로 삭제할 필요 없다.

## 로컬 검증

- `node --test tests/phone-lookup.test.mjs`: 4개 테스트. DB 모의 객체는 전체 조회를 호출하면 실패하며, 동등 조회·중복 번호·오래된 검색 필드·입력 오류·일치 없음 검증.
- `npx tsc --noEmit --incremental false`: 통과.
- `node --check scripts/backfill-phone-last4.mjs`: 통과.
- 실제 Firebase 쿼리/마이그레이션 및 운영 화면은 아직 검증하지 않았다.
