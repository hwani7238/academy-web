# 출석·수납 자동화 적용 안내

## 구현된 흐름

- `/operations`: 원장용 출석·수강 설정·청구/수납·알림 기록·기기 관리.
- `/check-in`: 등록된 아이폰에서 본인 또는 보호자 번호 뒷자리 4자리로 검색, 동명이 아닌 중복 번호 학생 선택, 출석 등록.
- `/operations/demo`: 가상 학생으로 출석부터 청구·수납까지 체험. 외부 API·Firebase를 호출하지 않으며 새로고침 시 초기화된다. `1234` → 김하늘 → 마지막 1회 차감 → 청구 생성 → 수납 기록 → 8회 추가를 체험할 수 있다.
- `/check-in/demo`: 별도의 가상 아이폰 출석 체험. 다른 체험 탭과 상태를 공유하지 않는다.
- 기본 1회 차감. 원장이 0~10회로 수정할 수 있으며 사유·비고는 선택 입력이다. 같은 학생은 한국 날짜 기준 하루 한 출석 기록이며 연속 수업은 해당 기록의 횟수를 수정한다.
- 수강권의 횟수·금액은 학생별 설정. 잔여 횟수 0 이하가 되면 진행 중 청구가 없는 경우 다음 수강권 청구를 한 번 생성한다.
- 수강료 요청과 실제 수납은 별도다. 현금·카드·지역화폐·계좌이체 및 부분 수납을 기록한다. 전액 수납 시 한 번만 횟수가 추가된다. 선납 및 미납으로 발생한 음수 잔여도 보존한다.
- 출석/차감/청구/알림 대기, 수납/횟수 추가를 각각 Firestore 트랜잭션으로 처리한다. 수납 요청 ID 및 학생-날짜 출석 ID로 중복 처리를 방지한다.
- 수강 설정을 수정해도 기존 청구 가격이나 잔여 횟수를 덮어쓰지 않는다. 출석 차감 취소로 잔여가 양수가 되면 진행 중 청구를 확인 대상으로 표시한다.

## 접근 권한

- 금융·출결 운영 API는 Firebase ID 토큰을 검증하고 users 문서의 `admin` 또는 `wonjang` 역할만 허용한다. 기존 `siljang` 역할에는 이 새 권한을 자동 부여하지 않는다.
- 모든 `ops*` 컬렉션은 클라이언트 직접 접근을 금지하며 서버 API로만 사용한다. 기존 students 하위의 넓은 staff 규칙과 분리했다.
- 기기 등록 코드는 난수 128비트, 10분 유효, 1회용이다. 등록 후 256비트 토큰을 HttpOnly/SameSite=Strict 쿠키에 보관한다. DB에는 토큰 해시만 저장한다. 운영 HTTPS에서는 Secure를 적용한다. 기기 권한은 90일 만료, 관리 화면에서 즉시 중지 가능하다.
- 기기별 분당 60회 제한과 요청 Origin 검증을 적용한다. 등록 기기 검색은 이름과 ID만 반환하며 연락처·피드백·수납 정보를 반환하지 않는다.
- 기존 공개 Firestore logs 읽기를 닫았다. 학부모 토큰 링크는 기존 report 서버 API를 사용하므로 유지된다. Firebase rules는 웹 배포와 별도로 배포해야 한다.
- 기존 피드백 알림 API는 승인 여부·작성자·담당 과목/배정 관계를 검증하고 수신번호·템플릿·링크를 서버에서 결정한다. 이전 화면을 계속 열어둔 강사는 배포 후 새로고침해야 한다.

## 아직 별도인 업무

- 결제선생 API 연동 및 카드 승인/지역화폐 수납 자동 확인은 연결 수단이 없어 포함하지 않았다. 현재 수납은 실제 결제를 원장이 확인한 뒤 기록한다.
- 기존 `/world`의 4자리 피드백 조회는 기존 동작을 유지한다. 이 기능의 별도 보호자 인증 도입은 후속 보안 작업이다. 신규 아이폰 출석 API는 해당 피드백 조회를 사용하지 않는다.
- 기존 Storage와 학생 문서의 광범위한 staff 권한 전체 개편, 환불/결제 취소, 엑셀 원본 이관은 이 변경에 포함하지 않았다. 이관에는 학생별 실제 잔여 횟수·요금과 미납/선납 기준이 필요하다.
- 한 학생의 여러 과목별 독립 수강권은 현재 모델에서 합산하지 않는다. 이번 요구대로 학생별 한 개 수강권을 관리한다.

## NHN 설정과 발송

기존 `NHN_APP_KEY`, `NHN_SECRET_KEY`, `NHN_SENDER_KEY`, Firebase Admin 환경변수를 재사용한다. 추가할 서버 환경변수:

- `NHN_ATTENDANCE_TEMPLATE`: 승인된 출석 알림 템플릿 코드. 치환 변수 `student_name`, `attendance_time`.
- `NHN_BILLING_TEMPLATE`: 승인된 수강료 안내 템플릿 코드. 치환 변수 `student_name`, `amount`, `lesson_count`. 계좌 안내/학원명 등은 승인 템플릿의 고정 문구로 설정한다. 결제선생의 개인별 결제 링크를 생성하는 기능은 아니다.
- `APP_ORIGIN`: 기존 피드백 링크의 운영 도메인. 기본값 `https://wheemusic.com`.
- `CRON_SECRET`: 알림 대기 복구 작업을 호출할 때 사용할 충분히 긴 비밀값.

출석 및 원장 작업 응답 후 Next.js `after`에서 큐를 처리하므로 학생은 NHN 응답을 기다리지 않는다. 각 작업은 5건씩, 외부 요청은 최대 8초로 제한한다. 요청 종료/장애에도 대기 기록은 DB에 남는다.

복구용으로 `/api/cron/send-notifications`에 매분 또는 수분 간격 GET을 보내는 스케줄러를 연결한다. `Authorization: Bearer <CRON_SECRET>`가 필수다. 기존 Vercel 플랜의 Cron 제한을 확인한 뒤 설정해야 하므로 현재 vercel.json의 운영 일정은 변경하지 않았다. 별도 스케줄러를 쓰거나 허용 플랜에서 아래 항목을 기존 crons에 추가한다:

```json
{ "path": "/api/cron/send-notifications", "schedule": "* * * * *" }
```

설정 누락은 `blocked`로 남고 설정 후 원장이 ‘설정 완료 후 다시 처리’를 누르면 다시 큐에 들어간다. `submitted`는 NHN 접수이며 수신자 전달 완료가 아니다. `unknown`, 오래된 `processing`, `failed`는 NHN 내역에서 확인해야 한다. 결과가 불명확한 항목은 자동 재전송하지 않는다. 현재 NHN 최종 수신 결과 콜백은 연결하지 않았다.

실제 자동 결제 안내는 각 학생의 ‘수강 횟수 소진 시 결제 안내도 자동 요청’ 설정을 켠 경우에만 발생한다. 기본값은 꺼짐이며 청구 대상만 생성된다.

## 배포 순서

1. 먼저 검토/체험 화면을 확인하고 운영 백업을 확보한다. Firebase 테스트 프로젝트에서 권한·트랜잭션·지정 테스트 수신자 발송을 검증한다.
2. 기존 전화번호 검색 변경은 `PHONE_LOOKUP_ROLLOUT.md` 순서대로 검색 필드부터 보완한다. 전체 패치를 먼저 운영 배포하면 검색 필드가 없는 기존 학생이 world 검색에서 빠질 수 있다.
3. Firebase 규칙과 웹 변경을 함께 계획한다. 사용자 역할은 임의 승격하지 않는다.
4. 서버 환경변수와 승인된 NHN 템플릿을 연결한다. 운영 데이터 수정 또는 실제 발송은 이번 로컬 검증에서 수행하지 않았다.
5. 원장 계정으로 학생별 수강권/현재 잔여/알림 수신자/출석 번호를 설정한다. 아이폰 기기를 등록한다. 청구 자동 안내는 소수 지정 학생으로 확인한 후 켠다.
6. 큐 복구 스케줄러를 설정하고 모니터링한다. `opsAudit`, 오래된 기기/페어링·알림 기록은 보관 정책에 맞춰 정리하는 운영 작업을 별도로 둔다.

## 검증 범위

- 20개 테스트 통과: 번호 검색, 트랜잭션 모의 DB의 중복 출석/부분 수납/선납/과납/수정/기기 등록, 승인·역할·Origin·기기 만료/중지/빈도 제한, NHN 모의 타임아웃·설정 누락.
- TypeScript 검사 및 전체 Next.js 배포 빌드 통과.
- 새로운 운영 기능과 테스트에 대한 ESLint 통과.
- `/operations/demo` 로컬 렌더 HTTP 200 확인. 브라우저 상호작용 QA, 실제 Firebase 규칙 에뮬레이터, 운영 DB 및 실제 NHN 발송은 미실행.

## 결제선생 연결 대기 변경

결제 안내는 API 계약·테스트 완료 전까지 blocked로 보관하며 NHN으로 대신 발송하지 않는다. 위 NHN_BILLING_TEMPLATE 설정은 더 이상 사용하지 않는다. configured는 출석 알림 설정만 의미한다. 날짜 조회는 상단에서 변경하고 오늘 버튼으로 복귀한다.

## 월별 출결 관리

- 조회 월의 데이터만 불러오는 월별 출석표 추가. 빈칸은 결석이 아닌 미기록이다.
- present/absent/makeup/cancelled와 차감 횟수를 분리한다. 기존 상태 없는 기록은 출석으로 표시한다.
- 결석 기본 0회, 보강 기본 1회는 입력 제안이며 원장이 저장 전 변경한다. 보강 원래 날짜는 참고 기록으로 원본 차감을 바꾸지 않는다.
- 수동 기록은 알림을 보내지 않는다. 변경 차이만 잔여에 반영하고 변경 이력을 남긴다. 같은 입력 재전송은 중복 차감하지 않으며 오래된 수정은 거부한다.
- 학생 한 명당 하루 한 기록. 기존 결석/취소가 있으면 아이폰은 원장에게 변경 요청을 안내한다.
- 실제 시트 이관·운영 데이터 연결·도메인 배포는 아직 하지 않았다.
